import './styles.css';
import {
    addTask,
    addCollection,
    add,
    tasksByTime,
    tasksByCollection,
    sideBar,
    viewContent,
    motherContainer,
} from './UI'


const main = motherContainer();

document.body.appendChild(main);